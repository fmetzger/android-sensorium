- FIX: sensor view does not immediately update after a preference change
- FEATURE: sensor data logging
- FEATURE: built-in data upload
- CHANGE: improve preference layout
- CHANGE: PrivacyHelper anonymization method improvements
- CHANGE: improve the way xmlrpc finds its methods in the sensors (e.g. remove @xmlrpcmethod and generate calls directly from SensorValue attributes)
- CHANGE: change name to Sensorium(?)
- FEATURE: SensorValue needs to support list values; the sensor views also need to be able to handle this